services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app # Maps your local code to the container for live reloading
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload